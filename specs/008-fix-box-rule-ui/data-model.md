# Data Model

No new data entities are introduced in this feature.
Existing models:
- `BoundingBox`: Modified usage in extraction strategies to account for `page.rect.x0` and `page.rect.y0`. No structural changes.
- `BoundingBoxExtractionRule`: Unchanged.
- `Template`: Unchanged.
